import { PaginatorInterceptor } from './paginator.interceptor';

describe('PaginatorInterceptor', () => {
  it('should be defined', () => {
    expect(new PaginatorInterceptor()).toBeDefined();
  });
});
